# Checkpoint G1.2g: Model-specific request review

**Status:** Approved

**Phase:** 1, Sponsor-stack and architecture feasibility

**Step:** 1.2, Prove the sponsor primitives independently

**Date submitted:** 2026-09-07

**Author:** Project team

**Reviewer:** Project owner

## Objective

Review the corrected Nemotron request and its hard execution boundaries before
one final live capability attempt.

## Scope completed

- Added NVIDIA's recommended tool-calling sampling values.
- Increased the attempt-specific output allowance to 10,000 tokens.
- Required that exact output allowance in the Nemotron execution preflight.
- Preserved one model call, one forced tool, one choice, disabled parallel tool
  calls, zero automatic retries, and the $0.01 cost ceiling.
- Retained early usage and structural diagnostics on contract failures.
- Verified the request and closed-gate behavior offline.
- Prepared attempt identifier `g1.2-nemotron-003` without sending it.

## Reviewed request

| Control | Value |
|---|---:|
| Model | `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B` |
| Maximum model calls | 1 |
| Maximum input tokens | 8,192 |
| Maximum output tokens | 10,000 |
| Temperature | 0.6 |
| Top-p | 0.95 |
| Maximum calculated cost | $0.01 |
| Worst-case calculated cost | $0.00289152 |
| Automatic retries | 0 |
| Parallel tool calls | Disabled |
| Timeout | 60 seconds |

## Acceptance criteria and evidence

| Criterion | Evidence | Result |
|---|---|---|
| NVIDIA tool-calling sampling is explicit | Request tests assert temperature 0.6 and top-p 0.95 | Pass |
| Output allowance matches the reviewed correction | Preflight and probe tests require 10,000 tokens | Pass |
| Worst-case cost stays below the hard ceiling | $0.00289152 is below $0.01 | Pass |
| Ordinary configuration cannot open the gate | The 1,024-token default fails Nemotron budget validation | Pass |
| Request cardinality remains bounded | One call, one choice, one forced tool, parallel calls disabled | Pass |
| Failure diagnostics remain safe | Tests retain usage and structural facts without generated values | Pass |
| Live access remains closed during review | No provider request or infrastructure operation was made | Pass |
| Complete offline suite passes | 54 tests, dependency, compilation, and preflight checks pass | Pass |

## Verification

The complete offline suite passed 54 tests. Dependency validation found no
broken requirements, and source and test bytecode compilation passed.

The preflight produced two distinct safe outcomes:

- the ordinary 1,024-token configuration kept the Nemotron live gate closed;
- the exact reviewed 10,000-token configuration opened only the Nemotron gate
  and reported a worst-case calculated cost of $0.00289152.

Both reports declared that they perform no provider calls and require review.
No live request or infrastructure operation ran during verification.

## Risks and boundaries

- A 10,000-token allowance is a maximum, not a target. The provider may use
  fewer tokens.
- A schema-valid tool call is still unproven until attempt 003 succeeds.
- The account usage display may remain rounded or delayed. The probe record
  therefore remains the immediate usage evidence.
- Approval authorizes no Tavily request and no Sandbox operation.

## Recommendation

Approve exactly one final Nemotron request using the reviewed values. Stop
after the response without a retry, regardless of outcome, then submit the
result for review.

## Review decision

**Outcome:** Approved

**Reviewer:** Project owner

**Decision date:** 2026-09-07

**Conditions or follow-up work:** Execute exactly one Nemotron request with the
reviewed values, save only the redacted result, stop without a retry regardless
of outcome, and submit the result for review. Do not invoke Tavily or Sandbox.

**Next authorized step:** Nemotron attempt 003 only
