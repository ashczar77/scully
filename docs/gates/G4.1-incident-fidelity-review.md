# Gate G4.1: Incident Fidelity Review

**Status:** Approved

**Review date:** 2026-09-12

**Decision:** Approved, authorize Step 4.2 only

**Decision owner:** Project owner

## Scope reviewed

Gate G3.5 authorized Step 4.1 only. This review covers the selected realistic
proxy incident, capsule revision, rights evidence, target signature, competing
hypotheses, expected and known-good observations, real local proxy execution,
downloadable fixture changes, and regression coverage.

It does not authorize Step 4.2 minimization, provider execution, deployment,
remote cancellation work, expanded incident support, or submission publishing.

## Fidelity decision table

| Criterion | Evidence | Result |
|---|---|---|
| Credible production-only failure | A direct Express application distinguishes clients, while the same requests behind an untrusted proxy collapse to its socket identity | Pass |
| Proxy boundary is real | Separate loopback proxy and Express servers exchange actual HTTP requests | Pass |
| Forwarded header provenance is credible | Only the proxy writes `X-Forwarded-For`; clients provide a validated test-only marker | Pass |
| Signature is stable | Three incident runs produced identical required and informational observations | Pass |
| Known-good is discriminating | Three one-variable trust changes produced identical `200,200` non-matches | Pass |
| Alternatives are plausible | Trust configuration, forwarded-chain handling, and limiter keying each explain the observed identity collapse and have one bounded test | Pass |
| Verdict is not a capsule-ID lookup | Branches execute HTTP behavior; a changed signature with the same capsule ID does not return a fixed winner | Pass |
| Capsule is rights-cleared | Project-created artifacts, TEST-NET-2 traffic, dependency license basis, and URL-only references are recorded | Pass |
| Restricted data is absent | Import scanning, evidence hashes, explicit exclusions, and a rights review cover all six files | Pass |
| Existing trust experience remains intact | Backend, frontend, type, build, package, and dependency checks pass | Pass |
| Provider boundary remains closed | No provider client, request, credit, cost, or control was introduced | Pass |

## Verification result

- Backend suite: 162 tests passed.
- Frontend suite: 6 tests passed.
- TypeScript type check: passed.
- Production frontend build: passed.
- Python bytecode compilation: passed.
- Installed dependency check: passed with no broken requirements.
- Standalone incident verification: passed.
- Incident stability: 3 identical runs.
- Known-good stability: 3 identical runs.
- Live provider requests: zero.
- Provider credits consumed: zero.

## Known limitations

1. The product supports one selected realistic incident.
2. The successful branch has not yet been minimized against the full case.
3. Clean-checkout and sponsor-stack repeatability remain pending.
4. Direct target-engineer validation remains pending.
5. Previously recorded provider and cancellation debt remains open.

## Recommendation

Approve Gate G4.1 and authorize Step 4.2 only: minimize the successful branch,
preserve the stable failure signature, and produce the independently runnable
result manifest and regression package required by the plan.

## Approval record

- Decision: Approved
- Approved by: Project owner
- Approval date: 2026-09-12
- Conditions: Proceed with Step 4.2 only. Preserve the reviewed failure
  signature, rights boundary, local execution restrictions, and explicit debt.
  Do not begin Step 4.3 or add provider execution, deployment, expanded
  incident support, or submission publishing without their own review gates.
