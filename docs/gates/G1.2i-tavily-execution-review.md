# Checkpoint G1.2i: Tavily execution review

**Status:** In review

**Phase:** 1, Sponsor-stack and architecture feasibility

**Step:** 1.2, Prove the sponsor primitives independently

**Date submitted:** 2026-09-07

**Author:** Project team

**Reviewer:** Project owner

## Objective

Review the bounded Tavily search and redacted result contract before one live
search is allowed.

## Scope completed

- Recorded the G1.2h approval and Nemotron capability decision.
- Implemented a single-search runner through the project adapter.
- Restricted the search to basic depth and `expressjs.com`.
- Disabled answers, raw content, images, and automatic parameters.
- Required usage metadata and a one-credit maximum.
- Added safe success and failure evidence records.
- Added the `scully-probe-tavily` project entry point.
- Ran the complete test suite without a live search.

## Acceptance criteria and evidence

| Criterion | Evidence | Result |
|---|---|---|
| Search input is public and bounded | Fixed 74-character documentation query | Pass |
| Search cost is deterministic | Basic depth, automatic parameters off, maximum one credit | Pass |
| Returned payload is minimized | Answers, raw content, and images disabled | Pass |
| Provenance is required | Probe requires at least one HTTP or HTTPS source URL | Pass |
| Search scope is constrained | Request and returned sources are limited to `expressjs.com` | Pass |
| Evidence excludes returned text | Runner retains URLs and measurements, not titles or snippets | Pass |
| Failure output is redacted | Tests omit provider messages and request identifiers | Pass |
| Request count is bounded | One synchronous search and no runner retry loop | Pass |
| Complete offline suite passes | 62 tests pass | Pass |
| Live gate remains closed | Tavily project API key is not configured | Pass |
| Other live providers remain unused | No request or infrastructure operation ran | Pass |

## Verification

The local editable package installed with the new `scully-probe-tavily` entry
point. Dependency validation found no broken requirements, bytecode compilation
passed, and the complete suite passed 62 tests.

The credential-safe preflight reported the exact Tavily budget as valid and
the credential as absent. The Tavily live gate therefore remained closed. The
preflight performed no provider calls.

## Remaining setup

The project owner must place a Tavily project API key in `TAVILY_API_KEY` in
the ignored `.env` file. This secret must not be pasted into chat or committed.
After that local change, the preflight must show the Tavily gate open before
execution.

## Recommendation

Approve the reviewed Tavily request. Once the project API key is configured,
approval authorizes exactly one search as attempt `g1.2-tavily-001`. Stop after
the response without a retry, then submit the redacted result at checkpoint
G1.2j. Do not run a Sandbox operation.

## Review decision

**Outcome:** Pending

**Reviewer:** Project owner

**Decision date:** Pending

**Conditions or follow-up work:** Pending review

**Next authorized step:** Pending review
